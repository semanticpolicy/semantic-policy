// Demo A - guard a prompt before it reaches the model. Not implemented yet.
// A probabilistic check is one layer of defence in depth, never a security boundary on
// its own; whatever lands here has to say so where a reader cannot miss it.
Console.Error.WriteLine("PromptInjectionGuard: not implemented yet.");
return 1;
